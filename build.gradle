apply plugin: 'java'
compileJava {
    sourceCompatibility = '11'
    targetCompatibility = '11'
}
compileJava.options.encoding = 'UTF-8'
javadoc.options.encoding = 'UTF-8'  

archivesBaseName = 'InMethodTools'
version = '2.04-20240601'


repositories {
   mavenCentral()
}


sourceSets {
    main {
        java {
            srcDir 'src'
        }

        resources {
            srcDir 'src'
        }        
    }
}

dependencies {
   compile fileTree(dir: 'lib', include: ['*.jar'])
}
